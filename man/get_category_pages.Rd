% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pages.R
\name{get_category_pages}
\alias{get_category_pages}
\title{Retrieve pages from a category}
\usage{
get_category_pages(category, limit = 500, handle = NULL, namespace = 0)
}
\arguments{
\item{category}{Character string containing the category name.
The category name must be stripped of the \code{"Category:"} prefix.}

\item{limit}{Maximum number of pages requested per API call.
The MediaWiki API may impose its own limits depending on user rights.}

\item{handle}{Optional \code{httr} handle used to maintain session state
across requests.}

\item{namespace}{Integer namespace identifier used to filter results.
Defaults to \code{0} (main namespace).}
}
\value{
A character vector containing page titles.
}
\description{
Retrieves the titles of pages belonging to a specified category using the
MediaWiki API.
}
\details{
Results are automatically paginated using the API continuation mechanism
until all matching pages have been retrieved.
}
\examples{
\dontrun{
# Retrieve all pages in Category:Water
pages <- get_category_pages("Water")

# Retrieve all pages in Category:Projects
projects <- get_category_pages("Projects")
}

}
\seealso{
\code{\link{get_all_pages}},
\code{\link{mediawiki_namespaces}}
}
